/**
 * React bindings for text-highlighter
 *
 * Provides a React hook for managing text highlights in a container.
 */

// Main hook
export { useHighlighter } from './useHighlighter';

// Types
export type {
    SyncableHighlight,
    UseHighlighterOptions,
    UseHighlighterReturn,
} from './types';
